export async function runCommand(plan: string, opts: { autoMerge?: boolean; model?: string; mode?: string }): Promise<void> {
  console.log(`plandriven run — not yet implemented (plan: "${plan}")`);
}
